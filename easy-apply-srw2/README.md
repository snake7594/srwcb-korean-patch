# 제2차 단독판(SLPS-02406) 간편 적용 도구

별매 단독판 CloneCD `.img` 용 더블클릭 설치기. `한글패치 적용하기.bat` 이 `apply.ps1`(실행정책 우회)을
실행 → SHA-256 검증 → xdelta 적용 → 결과 검증 → 단일 MODE2 데이터 트랙 `.cue` 생성.
원본 578,991,840B `.img`(sha a3d3a603). 결과 img sha acf1c391. 반드시 생성된 `.cue`로 열 것(.ccd 프리즈).
